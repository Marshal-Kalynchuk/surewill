<%= turbo_stream.remove "#{dom_id(@property)}_item" %>
<% if @properties.empty? %>
  <%= turbo_stream.update "properties_section" do %>
    <%= turbo_frame_tag "properties_section", src: user_will_properties_path(current_user) %>
  <% end %>
<% end %>