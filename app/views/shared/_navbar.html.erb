<nav id="navbar" >
  <div class="container">
    <ul>
      <li>
        <%= link_to "Home", home_path%>
      </li>
      <li>
        <%= link_to "About", about_path%>
      </li>
      <% if user_signed_in? %>
        <li> 
          <% if current_user.will && current_user.will.persisted? %>
            <%= link_to "View will", user_will_path(current_user) %>
          <% else %>
            <%= button_to "Create will", user_will_build_index_path(current_user) %>
          <% end %>
        </li>
        <li>
          <%= link_to "Account", user_path(current_user) %>
        </li>
      <% else %>
        <li>
          <%= link_to "Login", new_user_session_path %>
        </li>
        <li>
          <%= link_to "Sign Up", new_user_registration_path %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>