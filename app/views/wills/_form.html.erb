<%= form_for(@will) do |form| %>
  <% if will.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(will.errors.count, "error") %> prohibited this will from being saved:</h2>

      <ul>
        <% will.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :is_public, style: "display: block" %>
    <%= form.check_box :is_public %>
  </div>

  <div>
    <%= form.label :is_prepaid, style: "display: block" %>
    <%= form.check_box :is_prepaid %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
