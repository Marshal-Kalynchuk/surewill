<div class="container">
  <div class="header">
    <h1>Delegates</h1>
    <%= link_to "New delegate",
                new_user_will_delegate_path,
                class: "btn btn--primary",
                data: { turbo_frame: dom_id(Delegate.new) } %>
  </div>

  <%= turbo_frame_tag Delegate.new %>

  <%= turbo_frame_tag "delegates" do %>
    <%= render @delegates %>
  <% end %>
</div>